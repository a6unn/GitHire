# GitHire Frontend - Environment Variables

# API Base URL
# In development: Use /api (Vite proxy will forward to backend at localhost:8000)
# In production: Set to your backend API URL (e.g., https://api.githire.com/api)
VITE_API_BASE_URL=/api

# Optional: Enable/disable features
# VITE_ENABLE_ANALYTICS=false
# VITE_ENABLE_ERROR_TRACKING=false
